# Changesets

Run `pnpm changeset` to describe a change, then after merge the “Version packages” flow bumps versions and `CHANGELOG.md`. For npm publish from CI, see `../.github/workflows/release.yml` and [NEXT_STEPS.md](../NEXT_STEPS.md).
