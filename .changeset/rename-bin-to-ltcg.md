---
"limitless-cli": minor
---

Rename the npm package to `limitless-cli`, the CLI binary to `ltcg`, and the default config app id to `limitless-cli` (config file path changes for anyone who saved a token under the old id). `LIMITLESS_*` environment variables are unchanged.
