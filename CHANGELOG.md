# Changelog

## 0.2.0

- Remove Changesets tooling; bump versions manually and publish with `pnpm publish` (see [.github/workflows/release.yml](.github/workflows/release.yml) on pushing a `v*` tag).

## 0.1.0

- Initial release: `config`, `tournament` (list, get, standings, pairings), `game` (list, decks), global auth and output flags, official API base `https://play.limitlesstcg.com/api`.
