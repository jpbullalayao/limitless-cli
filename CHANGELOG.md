# Changelog

## 0.1.0

- Initial release: `config`, `tournament` (list, get, standings, pairings), `game` (list, decks), global auth and output flags, official API base `https://play.limitlesstcg.com/api`.

Future versions are managed with [Changesets](https://github.com/changesets/changesets); npm publish from CI is configured in `.github/workflows/release.yml`.
